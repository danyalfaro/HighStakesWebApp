import React, { Component } from "react";

export default class Landing extends Component {
  render() {
    return (
      <div>
        <h1>Welcome to High Stakes</h1>
        <button>Enable GeoLocation</button>
      </div>
    );
  }
}
